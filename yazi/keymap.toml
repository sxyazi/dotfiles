[manager]

keymap = [
	{ on = [ "<Esc>" ], exec = "escape"  },
	{ on = [ "q" ], exec = "quit" },
	{ on = [ "<C-q>" ], exec = "close" },

	# Navigation
	{ on = [ "u" ], exec = "arrow -1" },
	{ on = [ "e" ], exec = "arrow 1" },

	{ on = [ "U" ], exec = "arrow -5" },
	{ on = [ "E" ], exec = "arrow 5" },

	{ on = [ "n" ], exec = "leave" },
	{ on = [ "i" ], exec = "enter" },

	{ on = [ "N" ], exec = "back" },
	{ on = [ "I" ], exec = "forward" },

	# Selection
	{ on = [ "<Space>" ], exec = [ "select --state=none", "arrow 1" ] },
	{ on = [ "v" ], exec = "visual_mode" },
	{ on = [ "V" ], exec = "visual_mode --unset" },
	{ on = [ "<C-a>" ], exec = "select_all --state=true" },
	{ on = [ "<C-r>" ], exec = "select_all --state=none" },

	# Operation
	{ on = [ "<Enter>" ], exec = "open" },
	{ on = [ "<C-Enter>" ], exec = "open --interactive" },
	{ on = [ "y" ], exec = "yank" },
	{ on = [ "x" ], exec = "yank --cut" },
	{ on = [ "p" ], exec = "paste" },
	{ on = [ "P" ], exec = "paste --force" },
	{ on = [ "l" ], exec = "paste --follow" },
	{ on = [ "L" ], exec = "paste --follow --force" },
	{ on = [ "d" ], exec = "remove" },
	{ on = [ "D" ], exec = "remove --permanently" },
	{ on = [ "a" ], exec = "create" },
	{ on = [ "r" ], exec = "rename" },
	{ on = [ "h" ], exec = "hidden toggle" },
	{ on = [ "s" ], exec = "search fd" },
	{ on = [ "S" ], exec = "search rg" },
	{ on = [ "<C-s>" ], exec = "search none" },
	{ on = [ "z" ], exec = "jump zoxide" },
	{ on = [ "Z" ], exec = "jump fzf" },

	# Sorting
	{ on = [ ",", "a" ], exec = "sort alphabetical" },
	{ on = [ ",", "A" ], exec = "sort alphabetical --reverse" },
	{ on = [ ",", "c" ], exec = "sort created --reverse" },
	{ on = [ ",", "C" ], exec = "sort created" },
	{ on = [ ",", "m" ], exec = "sort modified --reverse" },
	{ on = [ ",", "M" ], exec = "sort modified" },
	{ on = [ ",", "s" ], exec = "sort size --reverse" },
	{ on = [ ",", "S" ], exec = "sort size" },

	# Tabs
	{ on = [ "t" ], exec = "tab_create --current" },

	{ on = [ "<C-n>" ], exec = "tab_switch -1 --relative" },
	{ on = [ "<C-i>" ], exec = "tab_switch 1 --relative" },

	{ on = [ "1" ], exec = "tab_switch 0" },
	{ on = [ "2" ], exec = "tab_switch 1" },
	{ on = [ "3" ], exec = "tab_switch 2" },
	{ on = [ "4" ], exec = "tab_switch 3" },
	{ on = [ "5" ], exec = "tab_switch 4" },

	{ on = [ "," ], exec = "tab_swap -1" },
	{ on = [ "." ], exec = "tab_swap 1" },

	# Tasks
	{ on = [ "w" ], exec = "tasks_show" },

	# Goto
	{ on = [ "g", "h" ], exec = "cd ~" },
	{ on = [ "g", "c" ], exec = "cd ~/.config" },
	{ on = [ "g", "d" ], exec = "cd ~/Downloads" },
	{ on = [ "g", "t" ], exec = "cd /tmp" },
	{ on = [ "g", "o" ], exec = "cd ~/Library/CloudStorage/OneDrive-SQUAREENIX" },
]

[tasks]

keymap = [
	{ on = [ "<C-q>" ], exec = "close" },
	{ on = [ "<Esc>" ], exec = "close" },
	{ on = [ "w" ], exec = "close" },

	{ on = [ "u" ], exec = "arrow -1" },
	{ on = [ "e" ], exec = "arrow 1" },

	{ on = [ "<Enter>" ], exec = "inspect" },
	{ on = [ "x" ], exec = "cancel" },
]

[select]

keymap = [
	{ on = [ "<C-q>" ], exec = "close" },
	{ on = [ "<Esc>" ], exec = "close" },
	{ on = [ "<Enter>" ], exec = "close --submit" },

	{ on = [ "u" ], exec = "arrow -1" },
	{ on = [ "e" ], exec = "arrow 1" },

	{ on = [ "U" ], exec = "arrow -5" },
	{ on = [ "E" ], exec = "arrow 5" },
]

[input]

keymap = [
	{ on = [ "<C-q>" ], exec = "close" },
	{ on = [ "<Enter>" ], exec = "close --submit" },
	{ on = [ "<Esc>" ], exec = "escape" },
	{ on = [ "<Backspace>" ], exec = "backspace" },

	# Mode
	{ on = [ "k" ], exec = "insert" },
	{ on = [ "a" ], exec = "insert --append" },
	{ on = [ "v" ], exec = "visual" },

	# Navigation
	{ on = [ "n" ], exec = "move -1" },
	{ on = [ "i" ], exec = "move 1" },

	{ on = [ "N" ], exec = "move -999" },
	{ on = [ "I" ], exec = "move 999" },

	{ on = [ "b" ], exec = "backward" },
	{ on = [ "w" ], exec = "forward" },
	{ on = [ "h" ], exec = "forward --end-of-word" },

	# Deletion
	{ on = [ "d" ], exec = "delete --cut" },
	{ on = [ "c" ], exec = "delete --cut --insert" },
	{ on = [ "x" ], exec = [ "delete", "move 1 --in-operating" ] },

	# Copy/Paste
	{ on = [ "y" ], exec = [ "yank" ] },
	{ on = [ "p" ], exec = [ "paste" ] },
	{ on = [ "P" ], exec = [ "paste --before" ] },

	# Undo/Redo
	{ on = [ "l" ], exec = [ "undo" ] },
	{ on = [ "L" ], exec = [ "redo" ] },
]
